# Define a lambda function to calculate the square of a number
square = lambda x: x * x

# Get input from the user
enter = int(input("Enter a number: "))

# Print the square of the entered number
print(f"The square of {enter} is {square(enter)}")
